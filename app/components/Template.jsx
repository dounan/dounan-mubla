import React, {Component, PropTypes} from 'react'

class Template extends Component {

  static propTypes = {
  };

  static defaultProps = {
  };

  componentWillMount() {
  };

  render() {
    return (
      <div>
      </div>
    );
  };
}

export default Template;

